# Copier ce fichier vers .env.development et ajuster les valeurs
# Utilisé automatiquement par `npm run dev`

# Base URL de l'API. En dev, `/api` est relayé vers FastAPI (port 8000) par le proxy Vite.
VITE_API_URL=/api
